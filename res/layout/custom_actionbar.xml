<?xml version="1.0" encoding="UTF-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/top_bar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/custom_actionbar_rlayout_height"
        android:layout_alignParentTop="true"
        android:background="#52357E" >

        <TextView
            android:id="@+id/actionbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/sign_in"
            android:textColor="#FFFFFF"
            android:textSize="@dimen/custom_actionbar_title_textSize"
            android:textStyle="normal" />

        <ImageView
            android:id="@+id/back"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentLeft="true"
            android:layout_centerVertical="true"
            android:layout_marginLeft="10dp"
            android:adjustViewBounds="true"
            android:contentDescription="@string/back"
            android:src="@drawable/back"
            android:clickable="true" />
        
        <ImageView
            android:id="@+id/ac_refresh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:layout_marginRight="16dp"
            android:adjustViewBounds="true"
            android:contentDescription="@string/back"
            android:src="@drawable/ic_action_refresh" 
            android:visibility="gone"/>

        <ImageView
            android:id="@+id/ac_search"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignTop="@+id/ac_refresh"
            android:layout_toLeftOf="@+id/ac_refresh"
            android:adjustViewBounds="true"
            android:contentDescription="@string/back"
            android:src="@drawable/ic_action_search"
            android:visibility="gone" />

    </RelativeLayout>